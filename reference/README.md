# Reference inputs

The original client-branded forms and populated inspection documents are intentionally not included in this public repository.

The PDF generator is structured around a typed intermediate model, so you can adapt the field layout to an authorized form without publishing the source document. Use synthetic inputs for local demos.
